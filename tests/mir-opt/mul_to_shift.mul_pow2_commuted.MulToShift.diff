- // MIR for `mul_pow2_commuted` before MulToShift
+ // MIR for `mul_pow2_commuted` after MulToShift
  
  fn mul_pow2_commuted(_1: i64) -> i64 {
      debug x => _1;
      let mut _0: i64;
      let mut _2: i64;
  
      bb0: {
          StorageLive(_2);
          _2 = copy _1;
-         _0 = Mul(const 16_i64, move _2);
+         _0 = Shl(move _2, const 4_i64);
          StorageDead(_2);
          return;
      }
  }
  
