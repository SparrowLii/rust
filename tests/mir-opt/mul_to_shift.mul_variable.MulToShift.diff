- // MIR for `mul_variable` before MulToShift
+ // MIR for `mul_variable` after MulToShift
  
  fn mul_variable(_1: u32, _2: u32) -> u32 {
      debug x => _1;
      debug y => _2;
      let mut _0: u32;
      let mut _3: u32;
      let mut _4: u32;
  
      bb0: {
          StorageLive(_3);
          _3 = copy _1;
          StorageLive(_4);
          _4 = copy _2;
          _0 = Mul(move _3, move _4);
          StorageDead(_4);
          StorageDead(_3);
          return;
      }
  }
  
