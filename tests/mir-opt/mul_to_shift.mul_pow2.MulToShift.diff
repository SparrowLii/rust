- // MIR for `mul_pow2` before MulToShift
+ // MIR for `mul_pow2` after MulToShift
  
  fn mul_pow2(_1: u32) -> u32 {
      debug x => _1;
      let mut _0: u32;
      let mut _2: u32;
  
      bb0: {
          StorageLive(_2);
          _2 = copy _1;
-         _0 = Mul(move _2, const 8_u32);
+         _0 = Shl(move _2, const 3_u32);
          StorageDead(_2);
          return;
      }
  }
  
